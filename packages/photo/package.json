{
  "name": "@opencut/photo",
  "version": "0.1.0",
  "private": true,
  "description": "Open Cut photo domain model: still-image document, layers, commands, versioned IO. Platform-agnostic; depends only on core.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@opencut/core": "0.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
